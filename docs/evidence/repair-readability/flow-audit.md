# End-to-end flow audit — 2026-09-25

## Scope and baseline

Read-only runtime audit against the source in `source/`, with current working-tree edits left untouched. I read `AGENTS.md`, the complete canonical prompt pack (`docs/agent_prompt_pack.md` and `docs/ai_agent_prompt_pack.md`), `docs/production_state.md`, `docs/agent_memory.md`, `docs/qa_current.md`, flow/release records and the flow, chapter, save and checkpoint tests. This report does not claim a fresh play session, new screenshots, or an exported release.

## Actual playable path

The player-facing path is implemented in `game/main.gd`: title Continue/New (`build_ui`, lines 586–597), frame/Blessing setup and departure preview (lines 615–638), run creation and departure checkpoint (`begin`, lines 261–273), automatic combat progression, shop and boss transitions (`game/simulation.gd`, `step`, lines 958–1004), site-clear memory and profile/checkpoint receipt (`game/main.gd`, lines 151–165; `game/simulation.gd`, lines 503–549), route choice and two road decisions (`game/main.gd`, lines 639–663; `game/simulation.gd`, lines 320–381), destination arrival and deliberate combat entry (`game/simulation.gd`, lines 382–440), terminal Results and return to setup (`game/main.gd`, lines 695–699), and fresh same-seed departure (`begin`, seed defaults to 147 and remains selectable in setup). Main mode is `optional` (`game/main.gd:32`), and destination boss defeat advances without repair completion (`game/simulation.gd:995–997`). The route graph is Workshop → Brass/Rootworks → Pale/Red/Null, with four valid three-site paths.

Recovery exists: health pickup/optional work and destination arrival floor; defeat produces causal Results (`game/simulation.gd:1120–1205`) and clears the resumable save through `game/main.gd:395–402`; Continue restores an atomic checkpoint through `load_run` (`game/main.gd:456–475`). `tests/test_flow_input.gd` traverses title → setup → shop → site clear → route → road → arrival → terminal Results → setup by UI actions, but shortcuts boss completion by setting `boss_dead` and objectives. `tests/test_chapter.gd` exercises the rules for multiple branches; `tests/test_save_flow.gd` and `tests/test_checkpoint_lifecycle.gd` exercise exact restoration and boundary persistence. `tests/run_four_path_pacing.gd` supplies controlled policy viability. These establish executable technical flow, not natural player comprehension.

## Prioritized findings

1. **P2, observed UI defect already assigned elsewhere:** Workshop optional-machine Warning Bell benefit clips at the right playfield edge. Reproduce with `../.runtime/godot/Godot_v4.5.1-stable_win64_console.exe --path . --resolution 1280x800 --script tests/capture_actor_art.gd`, then inspect `docs/evidence/prompt0-audit/FOREMAN_FINAL_ORDERS.png`. The unwrapped text originates in `game/main.gd:draw_optional_machines`; `docs/qa_current.md` records the inspected failure and `docs/task_packets/workshop_repair_readability.md` defines the smallest fix and acceptance. Parent/repair agent owns this fix, so this audit makes no overlapping edit.
2. **P1 release gate, no standalone exported player package in this workspace:** `Play Scrap Saint.cmd` is a functional local launcher that opens `project.godot` using `../.runtime/godot/Godot_v4.5.1-stable_win64.exe`; that executable exists. It needs the source tree and local Godot. `export_presets.cfg` and `scripts/build_release.sh` define a Windows export, PCK boot smoke and ZIP package, but the workspace has no `build/` directory or `ScrapSaint.exe`/PCK/ZIP. The release checklist requires a packaged Windows smoke. Smallest next release slice after interaction acceptance: build from a clean reviewed commit, launch the exported EXE without editor/dev flags, then exercise new run, Continue, defeat, and finale Results. No source change is implied by this finding.
3. **P1 creative acceptance gap:** No uncoached 1× full expedition, physical-controller session, audio listening review or sustained minimum-hardware rendered performance is recorded for this source in `docs/qa_current.md` or `docs/production_state.md`. The fixed-tick policy and direct-state UI tests cannot establish whether a new player can read the repair benefit, infer shop consequences, recognize bosses, or finish the four branches comfortably. The smallest acceptance check after the clipping fix is an uncoached 1× Workshop session, recording observed decisions and confusion; then widen to four paths only if Workshop acceptance holds. Treat this as unverified, not a code defect.
4. **P2 test evidence freshness/coverage:** Current QA's fresh 400 checks omit `tests/test_flow_input.gd`, `tests/test_chapter.gd`, `tests/test_save_flow.gd`, and `tests/run_four_path_pacing.gd` (`docs/qa_current.md:15–20`). Earlier records and both iteration runners include them, but they are not a fresh matrix for this tree. Before claiming end-to-end delivery, run these focused checks plus content validation after the repair edit, and inspect a natural run/capture. `tests/test_flow_input.gd` directly flips `boss_dead` and marks objectives complete, so add no new test merely to mirror its existing shortcut; use natural policy/human play for that missing evidence.

No additional reproducible main-mode progression or save blocker was established by this source review. Legacy relay mode still has mandatory objective rules, but it is gated behind the development toggle or old saves and does not describe the default optional-repair path.

## Verification and limits

Read-only commands: `rg --files`; `rg -n` over `game/main.gd`, `game/simulation.gd`, tests and runners; `Get-Content` for source, test and record ranges; `Get-ChildItem -Recurse -File -Include 'ScrapSaint.exe','ScrapSaint.pck','*.zip'` returned no export; `Get-ChildItem ../.runtime/godot` confirmed the local Godot executable. No Godot test or native capture ran during this parallel audit, and no runtime source changed. Existing parent/peer working-tree edits were left intact.

Exactly one next small task: finish the already-owned Workshop repair readability packet, including edge/large-text runtime capture inspection and focused flow/save regression, before the uncoached 1× Workshop acceptance session.
